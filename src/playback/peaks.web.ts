// Web stub: no native decoder, so the stylized waveform is always used.
export const canExtractPeaks = false;
export async function extractPeaks(_uri?: string, _bars?: number): Promise<number[] | null> {
  return null;
}
